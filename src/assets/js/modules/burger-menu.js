// Burger Menu Module
// DaisyUI drawer handles the menu toggle via checkbox - no JS needed
export function initBurgerMenu() {
    // DaisyUI drawer component handles menu toggle automatically
    // The burger button is now a <label> that controls the drawer-toggle checkbox
    return;
}
